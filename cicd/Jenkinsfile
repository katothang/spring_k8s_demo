pipeline {
    agent any

    tools {
        // Install the Maven version configured as "M3" and add it to the path.
        maven "M3"
    }

    stages {
        stage('checkout project') {
            steps {
                checkout scm
            }
        }
        stage('build project') {
            steps {
                // Run Maven on a Unix agent.
                sh "mvn clean install -DskipTests"
            }
        }
        stage('build Image') {
                    steps {
                        // Run Maven on a Unix agent.
                        sh "cd cicd"
                        sh "docker build - k8s_demo ."
                    }
                }
    }
}
